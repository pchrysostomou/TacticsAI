# TacticsAI — Dependencies
# PyTorch with CUDA is handled by setup_venv.ps1 (needs special index URL)
# Run: .\setup_venv.ps1  to install everything

# --- Core ML ---
ultralytics>=8.0.0          # YOLOv8
supervision>=0.21.0         # ByteTrack + annotation utilities

# --- Vision ---
opencv-python>=4.8.0        # Video I/O, drawing, perspective transforms

# --- Data / Math ---
numpy>=1.24.0
scipy>=1.11.0               # KDE for heatmaps (Week 3)
scikit-learn>=1.3.0         # K-Means for team classification (Week 2)

# --- Dashboard ---
streamlit>=1.28.0

# --- Utilities ---
Pillow>=10.0.0
tqdm>=4.65.0
